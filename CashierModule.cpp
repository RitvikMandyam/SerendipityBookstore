#include <iostream>
#include <algorithm>
#include <cctype>
#include <string>
#include <fstream>

using namespace std;
void pointOfSale() {
	string input;
	cout << "Enter the ISBN of a book to purchase (or type exit to leave): ";
	getline(cin, input);
	while (input != "exit") {

	}
}